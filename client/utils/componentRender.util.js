import clone from 'clone';
import store from './../store';

const componentRender = (components) => {
  console.log('in componentRender.util')
  console.log(components)
}

export default componentRender;